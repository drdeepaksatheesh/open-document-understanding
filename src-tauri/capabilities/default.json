{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum permissions for the v0.0.1 local PDF reader shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
